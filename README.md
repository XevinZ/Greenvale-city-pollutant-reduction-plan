To use just download R and Rstudio.
Install the necessary R packages.
Type and enter these on the terminal:
install.packages("shiny"),
install.packages("shinydashboard"),
install.packages("DT"),
Then Run the App!.
